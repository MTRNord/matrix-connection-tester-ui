# Sentry Configuration for Client-side (Browser)
# These FRESH_PUBLIC_ prefixed variables are inlined into the client bundle
# Replace YOUR_PROJECT_ID with your actual Sentry project ID from sentry.mtrnord.blog
FRESH_PUBLIC_SENTRY_DSN=https://sentry.mtrnord.blog/api/YOUR_PROJECT_ID/store/
FRESH_PUBLIC_ENVIRONMENT=local
FRESH_PUBLIC_APP_VERSION=local
